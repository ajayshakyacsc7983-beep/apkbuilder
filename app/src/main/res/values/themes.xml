<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyAw" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_purple</item>
        <item name="colorPrimaryVariant">@color/primary_light</item>
        <item name="colorOnPrimary">@color/text_white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/accent_neon</item>
        <item name="colorSecondaryVariant">@color/accent_neon</item>
        <item name="colorOnSecondary">@color/bg_dark_purple</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/bg_dark_purple</item>
        <!-- Customize your theme here. -->
    </style>
</resources>